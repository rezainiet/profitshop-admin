import React from 'react';

const Loading = () => {
    return (
        <div className='flex items-center justify-center min-h-screen'>
            <button className="btn btn-square loading btn-secondary"></button>
        </div>
    );
};

export default Loading;